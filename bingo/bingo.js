const arr = ["one", "two", "three", "four"];
let how = arr.filter(ar => ar === "one");
console.log(how);

const numbers=[200, 50, 100];
document.getElementById("demo").innerHTML = numbers.reduce(noNo)

function noNo(total, num) {
    return total - num;
};

let i = 0;
while(i <= 10 ) {
    console.log(i);
    i++
}
